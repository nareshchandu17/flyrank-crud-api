# Copy this file to .env and adjust values if needed
# When running with Docker Compose, use host=db
DATABASE_URL=postgres://flyrank:flyrank123@db:5432/flyrank_db
PORT=3000

# When running locally with npm run dev (outside Docker), use:
# DATABASE_URL=postgres://flyrank:flyrank123@localhost:5433/flyrank_db
